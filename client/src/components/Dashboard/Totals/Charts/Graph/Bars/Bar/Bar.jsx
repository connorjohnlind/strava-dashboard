import React, { Component } from 'react';
import PropTypes from 'prop-types';

import classes from './Bar.scss';

const Bar = (props) => {
  return (
    <div {...props}></div>
  );
};

export default Bar;
